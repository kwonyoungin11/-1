{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "TradingSafety": {
    "AllowLiveOrders": false,
    "KillSwitch": true,
    "OrderMode": "DryRun",
    "Comment": "UI host hard-codes safe defaults in DI. Do not put secrets here."
  },
  "Toss": {
    "AllowLiveHttp": false,
    "Comment": "Mock read-only only in this host. No client secrets in config."
  }
}
